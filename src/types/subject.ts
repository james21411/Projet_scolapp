export interface Subject {
  isActive: boolean;
    classId: string;
  schoolYear: string;
  id: string;
  name: string;
  code: string;
  category: string;
  coefficient: number;
  maxScore: number;
  // Ajoutez d’autres propriétés selon vos besoins
}
